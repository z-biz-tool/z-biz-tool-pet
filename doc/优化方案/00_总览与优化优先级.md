# Z-Bot 桌面宠物 — 总览与优化优先级

> **状态：设计方案，未实现；基线 `cfe55d1`；当前事实=静态代码证据，指标=拟定验收目标**

## 1. 项目定位与独特价值

Z-Bot 是一款 **Electron 桌面宠物助手**，核心价值区别于通用 AI 对话工具：

- **常驻陪伴**：透明置顶小窗（200×320）+ 系统托盘，始终可见、不打扰
- **养成情感连接**：6 维属性（hunger/happiness/energy/cleanliness/health/affection）+ 4 阶段生命周期 + 19 种情绪 + 离线衰减
- **语音优先交互**：按住说话 → whisper.cpp 本地 STT → AI 对话 → edge-tts 语音回复 → 音量打断
- **MCP 工具扩展**：11 个工具（web_search/read_url/get_weather/screenshot_analyze/execute_command 等），部分需用户确认
- **会议转录**：ffmpeg 录音 → 10 秒分块 STT → 滚动摘要 → 最终总结

对标 Desktop Pet / Shimeji 理念，但集成 AI 对话与工具调用能力。

## 2. 技术栈确认

| 层级 | 技术 | 证据 |
|------|------|------|
| 框架 | Electron 41.2.1 | `package.json` dependencies |
| 渲染 | React 19 + Vite 8 + TypeScript + Antd 6 + Zustand | `package.json`, `vite.config.ts` |
| 主进程 | CommonJS, ES2020 | `tsconfig.main.json` |
| Preload | contextBridge, contextIsolation:true | `src/preload/index.ts` |
| 后端服务 | Express (STT:8084, TTS:8086) | `server/stt.js`, `server/tts.js` |
| 语音识别 | whisper.cpp CLI 子进程 | `server/stt.js` L67 |
| 语音合成 | Python edge-tts（联网微软服务） | `server/tts_local.py` L8 |
| 构建 | tsc + vite build（无 electron-builder 配置） | `package.json` scripts |

**非 Tauri**：无 `src-tauri/`、无 `Cargo.toml`、无 `capabilities/` 目录。

## 3. 架构概览

```
┌─────────────────────────────────────────────────────┐
│  Electron Main Process (src/main/index.ts, 1471行)   │
│  ├─ AI Providers (ollama/openai/claude/gemini/...)  │
│  ├─ MCP Tools (11个工具, 部分需确认)                  │
│  ├─ 宠物养成/记忆/任务调度/成就/人设/场景/粒子         │
│  ├─ 会议转录 (ffmpeg + STT 分块)                     │
│  └─ spawn STT/TTS Express 子进程                     │
├─────────────────────────────────────────────────────┤
│  Preload (src/preload/index.ts, 252行)               │
│  └─ contextBridge 暴露 ~50 个 API                    │
├────────────────────┬────────────────────────────────┤
│  Pet Renderer      │  Admin Renderer                 │
│  (200×320 透明窗)   │  (1200×800 管理窗)              │
│  动画/拖拽/语音/情绪 │  对话/设置/会议/工具确认          │
├────────────────────┴────────────────────────────────┤
│  Server (Express 子进程)                              │
│  ├─ STT :8084 (whisper.cpp CLI)                     │
│  └─ TTS :8086 (Python edge-tts, 联网!)               │
└─────────────────────────────────────────────────────┘
```

## 4. 立即 P0（阻塞性缺陷）

| # | 问题 | 影响 | 详见 |
|---|------|------|------|
| P0-1 | `app.commandLine.appendSwitch('--no-sandbox')` 关闭沙箱 | 渲染进程逃逸风险 | [01§2.1](./01_现状审计与问题清单.md#21-已确认缺陷) |
| P0-2 | `execute_command` 工具 + task-system `execAsync` 无确认 | AI 可执行任意 shell | [04§3](./04_数据安全与可靠性.md#3-危险操作与-agent-约束) |
| P0-3 | `fileRead/fileReadAsBase64` 任意路径无白名单 | 读取系统敏感文件 | [04§2](./04_数据安全与可靠性.md#2-preload-最小暴露) |
| P0-4 | edge-tts 联网微软服务，违背"语音隐私本地化" | 语音数据外泄 | [04§4](./04_数据安全与可靠性.md#4-语音数据隐私) |
| P0-5 | `main/index.ts` L1039 引用未定义 `followUpResponse` | 运行时 ReferenceError | [01§2.1](./01_现状审计与问题清单.md#21-已确认缺陷) |
| P0-6 | `server/temp_audio.webm` 残留（45.6KB 已提交） | 用户音频隐私泄露 | [04§4](./04_数据安全与可靠性.md#4-语音数据隐私) |

## 5. 分阶段能力演进

- **Phase 1（安全加固）**：修复 P0、关闭 no-sandbox、工具确认机制、密钥加密
- **Phase 2（语音本地化）**：替换 edge-tts 为本地 TTS、whisper.cpp 模型管理、流式 STT
- **Phase 3（性能与体验）**：动画渲染优化、IPC 降频、内存控制、流式 AI 回复
- **Phase 4（产品完善）**：多显示器适配、开机自启、自动更新、跨平台隐身
- **Phase 5（工程化）**：electron-builder 打包、CI/CD、测试覆盖、代码清理

## 6. 文档索引

| 编号 | 文档 | 内容 |
|------|------|------|
| 01 | [现状审计与问题清单](./01_现状审计与问题清单.md) | 35+ 项已确认缺陷/待验证假设/已存在能力 |
| 02 | [产品能力与交互优化](./02_产品能力与交互优化.md) | 桌面宠物完整流程缺口与优先级 |
| 03 | [技术架构与性能优化](./03_技术架构与性能优化.md) | Electron 职责边界、whisper 集成、动画性能 |
| 04 | [数据安全与可靠性](./04_数据安全与可靠性.md) | 安全隔离、语音隐私、权限、密钥 |
| 05 | [实施路线与任务拆解](./05_实施路线与任务拆解.md) | 5 阶段文件级改造、依赖、回滚 |
| 06 | [测试验收与风险清单](./06_测试验收与风险清单.md) | 测试矩阵、量化验收、风险登记 |
