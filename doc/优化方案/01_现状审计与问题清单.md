# 现状审计与问题清单

> **状态：设计方案，未实现；基线 `cfe55d1`；当前事实=静态代码证据，指标=拟定验收目标**

## 1. 审计范围

基于静态源码阅读，覆盖：`src/main/`（12 文件）、`src/preload/`、`src/renderer/pet/`、`src/renderer/admin/`、`src/renderer/shared/`、`src/agent/`、`server/`（4 文件）、`whisper.cpp/`（空）、根配置文件。

## 2. 问题分类

### 2.1 已确认缺陷

| # | 严重度 | 文件（相对 `../../`） | 行号 | 描述 |
|---|--------|----------------------|------|------|
| D01 | P0 | `src/main/index.ts` | L33-37 | `--no-sandbox` / `--disable-gpu-sandbox` 关闭 Chromium 沙箱，渲染进程逃逸风险 |
| D02 | P0 | `src/main/index.ts` | L1039 | 引用未定义变量 `followUpResponse`，工具调用后 AI 追问分支必触发 ReferenceError |
| D03 | P0 | `src/main/task-system.ts` | L150-158 | command 类型任务直接 `execAsync(action.content)` 无任何确认，AI 可执行任意 shell |
| D04 | P0 | `src/main/mcp-tools.ts` | L233-262 | `openAppTool` 用字符串拼接 `open -a "${appName}"`，命令注入风险 |
| D05 | P0 | `src/main/index.ts` | L1183-1208 | `file:read` / `file:readAsBase64` 接受任意路径，无白名单/sandbox 限制 |
| D06 | P0 | `server/tts_local.py` | L8 | `edge_tts.Communicate` 联网微软服务，用户语音文本外泄 |
| D07 | P0 | `server/stt.js` | L24-27, L49 | `temp_audio.webm` 写入 server/ 目录且删除被注释掉；仓库已提交 45.6KB 残留音频 |
| D08 | P1 | `src/main/index.ts` | L971-974 | 工具确认 IPC 发送 `data.toolCallId`，但 admin `App.tsx` L227 解构 `data.id`，字段不匹配 |
| D09 | P1 | `src/renderer/admin/App.tsx` | L624 | 调用 `toolsCancel()` 但 `src/preload/index.ts` 未暴露该方法，运行时 undefined |
| D10 | P1 | `src/main/meeting-transcriber.ts` | L332 | POST `/inference` 但 `server/stt.js` 只有 `/transcribe` 路由，会议转录 STT 必 404 |
| D11 | P1 | `src/main/memory-system.ts` | L59 | `saveLongTermMemory` 引用模块作用域未定义的 `dataDir`，持久化必失败 |
| D12 | P1 | `src/main/task-system.ts` | L121-148 | speak/animation/changeScenery/petAction 用 `process.send`，main 非子进程，`process.send` 为 undefined |
| D13 | P1 | `src/renderer/pet/App.tsx` | L452-497 | `handleVoiceInput` 直接 fetch ollama URL，绕过主进程 `ai:chat`，与 admin 行为不一致、无工具调用 |
| D14 | P1 | `src/renderer/admin/App.tsx` | L442-571 | `sendMessage` 同样直接 fetch ollama，绕过主进程统一 AI 路由 |
| D15 | P1 | `src/main/index.ts` | L448-521 vs L745-816 | 托盘菜单构建代码完全重复（~70 行），维护不一致风险 |
| D16 | P2 | `src/main/index.ts` | L630-665 | 贴墙滑下用 `setInterval` 30ms 在主进程执行窗口动画，阻塞事件循环 |
| D17 | P2 | `src/renderer/pet/App.tsx` | L250-273 | 鼠标追踪 100ms `setInterval` 调用 IPC `getCursorPosition`，高频跨进程通信 |
| D18 | P2 | `src/main/index.ts` | L285-297 | 截图隐身 `applyStealthMode` 仅 macOS（`process.platform === 'darwin'`），Win/Linux 无实现 |
| D19 | P2 | `src/main/meeting-transcriber.ts` | L302-316 | `extractAudioChunk` 每次复制整个 wav 文件，非增量截取 |
| D20 | P2 | `src/main/index.ts` | L827-856 | 截图保存到 `app.getPath('temp')`，文件名含时间戳，无自动清理机制 |
| D21 | P2 | `whisper.cpp/` | — | 目录为空（0 items），无模型文件、无编译产物，STT 服务启动必失败 |
| D22 | P2 | `src/main/index.ts` | 全局 | `loadConfigFromFile()` 每次 IPC 调用都从磁盘同步读取，无内存缓存 |
| D23 | P2 | `src/main/index.ts` | L1051-1065 | 历史持久化用 `fs.writeFileSync` 同步写，阻塞主进程 |
| D24 | P2 | `src/agent/AgentManager.ts` | L40-181 | SQL Agent 全部 TODO mock，与桌面宠物无关，是 z-biz-tool-db 残留代码 |
| D25 | P2 | `src/renderer/global.d.ts` | L254, L276 | 声明 `toolsCancel` / `sendNotification` 但 preload 未实现，类型与运行时不一致 |

### 2.2 待验证假设

| # | 假设 | 验证方法 |
|---|------|----------|
| H01 | `aiApiKey` 明文存储于 `~/.z-bot/config.json`，无加密 | 运行时检查文件内容 |
| H02 | 全局快捷键 `Cmd+Shift+Z` / `Alt+Shift+V` 可能与其他应用冲突 | 多应用并行测试 |
| H03 | petWindow 200×320 在 HiDPI/多显示器下可能模糊或越界 | 不同分辨率实测 |
| H04 | STT 服务无鉴权，局域网内其他设备可调用 :8084 | 网络扫描验证 |
| H05 | 语音打断阈值 `INTERRUPT_THRESHOLD=30` 在嘈杂环境误触发 | 多环境实测 |
| H06 | `cron-parser` 任务调度在系统休眠后恢复时可能堆积执行 | 休眠/唤醒测试 |
| H07 | Electron 41 的 `--disable-gpu` 在 Apple Silicon 上可能导致软件渲染高 CPU | 活动监视器验证 |

### 2.3 已存在能力（正面确认）

| # | 能力 | 证据 |
|---|------|------|
| C01 | contextIsolation:true + nodeIntegration:false | `src/main/index.ts` L317-321, L364-368 |
| C02 | 双窗口架构（admin + pet）独立渲染 | `src/main/index.ts` createAdminWindow / createPetWindow |
| C03 | 18 种 CSS 动画 + 优先级打断机制 | `src/renderer/pet/animations.ts` 全 377 行 |
| C04 | 19 种情绪系统 + AI 回复关键词提取 | `src/renderer/shared/emotions.ts` 全 180 行 |
| C05 | 7 套预设皮肤 + AI 自定义皮肤生成 | `src/renderer/shared/skins.ts` 全 183 行 |
| C06 | MCP 工具确认机制（60 秒超时） | `src/main/index.ts` L959-980 |
| C07 | 宠物养成 6 维属性 + 离线衰减 + 4 阶段 | `src/main/index.ts` L1335-1396 |
| C08 | 记忆系统（短期 50 条 + 长期文件 + 正则提取） | `src/main/memory-system.ts` 全 172 行 |
| C09 | 多 AI 提供商统一接口（6 内置 + custom） | `src/main/ai-providers.ts` 全 810 行 |
| C10 | 会议转录完整流程（录音→分块→STT→摘要→总结） | `src/main/meeting-transcriber.ts` 全 355 行 |
| C11 | 语音打断（MediaRecorder + AudioContext 音量检测） | `src/renderer/pet/App.tsx` L289-327 |
| C12 | 全局快捷键 + 按住说话（Push-to-Talk） | `src/main/index.ts` L1163-1180 |
| C13 | 截图隐身模式（macOS Mission Control 隐藏） | `src/main/index.ts` L285-297 |
| C14 | 场景系统（day/night/rain/sunset）+ 时间自动切换 | `src/main/scenery-system.ts` 全 182 行 |
| C15 | 成就系统 12 项 + 人设系统 6 套 | `src/main/achievement-system.ts`, `persona-system.ts` |

## 3. 工程化缺失

| 项目 | 现状 |
|------|------|
| 测试 | 无任何测试文件/目录 |
| CI/CD | `.github/workflows/` 为空 |
| 构建脚本 | `scripts/` 为空；无 electron-builder 配置 |
| 代码规范 | 无 ESLint/Prettier 配置 |
| 类型安全 | `global.d.ts` 与 preload 实际暴露不一致 |
| 依赖管理 | `whisper.cpp/` 空目录，模型未版本化 |
| 文档 | TASK.md 仅 2 条声明，README.md 50 行基础说明 |

## 4. 量化基线（拟定，待实测确认）

| 指标 | 当前估计 | 目标 |
|------|----------|------|
| 常驻 CPU（idle） | 3-8%（100ms IPC 轮询 + setInterval） | < 1% |
| 常驻内存 | 300-500MB（Electron 双窗口 + 2 Express 子进程） | < 200MB |
| 语音识别延迟 | 2-5s（webm→wav→whisper tiny） | < 1.5s |
| AI 首字延迟 | 1-3s（非流式，等完整响应） | < 500ms（流式） |
| 启动时间 | 未测量 | < 3s |
| 拖拽帧率 | 未测量（mousemove + IPC） | ≥ 30fps |
